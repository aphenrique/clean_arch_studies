import 'package:clean_arch_studies/features/domain/usecases/get_space_media_usecase.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  GetSpaceMediaUsecase usecase;

  setUp(() {
    usecase = GetSpaceMediaUsecase();
  });

  test('shoud get space media entity for a given date from the repositiry',
      () {});
}
